class Solution {
public:
    int poorPigs(int buckets, int minutesToDie, int minutesToTest) {
        int states = minutesToTest / minutesToDie + 1;
        int pigs = 0;
        int cover = 1;

        while (cover < buckets) {
            pigs++;
            cover *= states;
        }

        return pigs;
    }
};
